/submit/expense/
    POST, returns a json 
    input: data(optional), text, amount, user 
    outpot: status: ok


/submit/expense/
    POST, returns a json 
    input: data(optional), text, amount, user 
    outpot: status: ok

